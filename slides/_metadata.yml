# Note: Each slide file defines its own format: revealjs with full options
# slides/index.qmd uses format: html to render as a listing page
# This file intentionally has no format settings to avoid conflicts
_placeholder: true
